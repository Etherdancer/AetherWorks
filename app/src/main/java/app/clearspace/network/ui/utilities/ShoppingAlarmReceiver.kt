package app.clearspace.network.ui.utilities

import android.content.BroadcastReceiver
import android.content.Context
import android.content.Intent

/**
 * Placeholder implementation for the ShoppingAlarmReceiver referenced in the AndroidManifest.
 * The real implementation should handle alarm events for shopping reminders.
 */
class ShoppingAlarmReceiver : BroadcastReceiver() {
    override fun onReceive(context: Context?, intent: Intent?) {
        // No‑op placeholder – simply log receipt if needed.
    }
}
